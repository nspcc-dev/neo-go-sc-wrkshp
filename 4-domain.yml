name: "Domain contract"
supportedstandards: []
events:
  - name: registered
    parameters:
      - name: address
        type: ByteString
      - name: domainName
        type: ByteString
  - name: deleted
    parameters:
      - name: address
        type: ByteString
      - name: domainName
        type: ByteString
